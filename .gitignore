# Python
.venv/
__pycache__/
*.pyc

# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log

# Ansible
*.retry

# Secrets — 改明文後, 真正的 lab.yaml 絕不可 commit (原 sops 設計才 commit 加密版)
inventory/secrets/lab.yaml
inventory/secrets/*.dec.*
inventory/secrets/*.plain.*
!inventory/secrets/.gitkeep
!inventory/secrets/lab.example.yaml

# Age 私鑰絕對不能進 git
keys.txt
*.age.key
age-key*

# PowerCLI logs
batch-*.csv
*.log

# 編輯器
.DS_Store
.vscode/settings.json
*.swp
*~

# ISO / depot (大檔不入 git)
*.iso
*-depot.zip
